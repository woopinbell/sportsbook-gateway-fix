<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        ADR-0015 baseline: Java 17 + Spring Boot 3.2.x + Maven, inheriting the
        Boot BOM via spring-boot-starter-parent..

        The application combines Spring Cloud Gateway and STOMP WebSocket. The reactive gateway (spring-cloud-
        starter-gateway) forces the WebFlux/Netty stack, but Spring's STOMP
        broker (@EnableWebSocketMessageBroker) is servlet-only. They cannot
        coexist. Reconciliation: Spring Cloud Gateway *Server MVC*
        (spring-cloud-starter-gateway-mvc) keeps the gateway product on the
        servlet stack, so servlet STOMP coexists and the stack stays consistent
        with betting/wallet (also Spring MVC). The cost is that proxying is a
        blocking RestClient call rather than reactive — acceptable for a front
        door whose downstreams are all MVC.
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.11</version>
        <relativePath/>
    </parent>

    <groupId>com.sportsbook</groupId>
    <artifactId>gateway</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>gateway</name>
    <description>
        Public API gateway for the sportsbook system — the single entry point
        for external clients. Verifies RS256 JWTs (first-line auth), enforces
        per-user / per-IP rate limits (Bucket4j + Redis), routes REST traffic to
        betting / wallet / odds-feed (Spring Cloud Gateway Server MVC), and
        pushes live odds + bet-status updates over STOMP WebSocket fed by Kafka.
        No domain logic, no balance mutation, no direct datastore access
        (ADR-0004 / ADR-0007 / ADR-0011).
    </description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Spring Cloud train for Boot 3.2.x. Provides the servlet gateway
             (spring-cloud-starter-gateway-mvc). -->
        <spring-cloud.version>2023.0.3</spring-cloud.version>

        <!-- Aligned with shared-protocol/pom.xml so the Avro generated classes
             (OddsChanged / BetSettled / BetVoided) link cleanly here.
             ADR-0014 / ADR-0006. 0.2.0 carries the settlement result events. -->
        <shared-protocol.version>0.2.0-SNAPSHOT</shared-protocol.version>
        <avro.version>1.12.0</avro.version>
        <testcontainers.version>1.20.3</testcontainers.version>
        <!-- Pinned because annotationProcessorPaths does not consult the
             Spring Boot BOM; keep in sync with the BOM-managed version. -->
        <lombok.version>1.18.30</lombok.version>

        <!-- Bucket4j: token-bucket rate limiting with a Redis (Lettuce) backing
             store for cross-instance limits. Not managed by the Boot BOM, so
             pinned here. 8.9.0 is the last line with the classic
             bucket4j-core / bucket4j-redis artifact names. -->
        <bucket4j.version>8.9.0</bucket4j.version>

        <!-- Self-contained (shaded) WireMock so the downstream HTTP stubs in
             routing tests do not collide with the app's Jetty / Jackson. -->
        <wiremock.version>3.9.2</wiremock.version>

        <spotless.version>2.43.0</spotless.version>
        <checkstyle.plugin.version>3.5.0</checkstyle.plugin.version>
        <checkstyle.version>10.18.2</checkstyle.version>

        <!-- @Tag("load") tests are skipped by default; override empty to run them:
             mvn test -DexcludedGroups= -Dtest=*LoadTest -->
        <surefire.excludedGroups>load</surefire.excludedGroups>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Cross-repo contracts: Money / Odds value types, ErrorCode, and the
             Avro-generated OddsChanged / BetSettled / BetVoided records the
             WebSocket bridge decodes off Kafka. Resolved from mavenLocal. -->
        <dependency>
            <groupId>com.sportsbook</groupId>
            <artifactId>shared-protocol</artifactId>
            <version>${shared-protocol.version}</version>
        </dependency>

        <!-- Spring Cloud Gateway Server MVC: servlet-stack gateway. Brings
             spring-webmvc; routes are RouterFunctions with filters. -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway-mvc</artifactId>
        </dependency>

        <!-- Security: RS256 JWT verification at the edge (ADR-0011). The
             resource-server starter auto-configures a NimbusJwtDecoder from the
             configured RSA public key. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- WebSocket / STOMP: the gateway is itself the STOMP broker for live
             odds + bet-status fan-out (servlet stack, coexists with gateway-mvc). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- Rate limiting: Bucket4j token buckets backed by Redis (Lettuce) for
             cross-instance limits. data-redis provides the Lettuce client. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j-redis</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Messaging: ADR-0006 (Kafka) + ADR-0014 (Avro 1.12.x, schema files
             owned by shared-protocol; no Schema Registry in V1). The gateway is
             a pure consumer: odds.changed -> STOMP /topic/odds, bet.settled /
             bet.voided -> per-user bet-status push. -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.avro</groupId>
            <artifactId>avro</artifactId>
            <version>${avro.version}</version>
        </dependency>

        <!-- Observability: ADR-0007. JSON structured logs + OTel exporter +
             Prometheus scrape via Actuator. -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>7.4</version>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Lombok: boilerplate trim alongside Java 17 records (ADR-0015). -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test stack: ADR-0015 — JUnit5 + AssertJ + Mockito + Testcontainers. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Redis for rate-limit integration tests runs via a GenericContainer
             (redis:7), so no extra Testcontainers module is needed. -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- WireMock: stubs betting / wallet / odds-feed HTTP in routing tests. -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>${wiremock.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Skip @Tag("load") tests in the normal build; run the throughput
                 harness explicitly with `mvn test -DexcludedGroups= -Dtest=*LoadTest`. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>${surefire.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- Spotless: google-java-format (GOOGLE style, same version as the
                 other services). Runs on verify; `mvn spotless:apply` formats. -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <googleJavaFormat>
                            <version>1.22.0</version>
                            <style>GOOGLE</style>
                        </googleJavaFormat>
                        <removeUnusedImports/>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <goals><goal>check</goal></goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Checkstyle: shared semantic rule set (magic numbers, parameter
                 counts, unused imports). Spotless owns layout. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${checkstyle.plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>config/checkstyle/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failOnViolation>true</failOnViolation>
                    <includeTestSourceDirectory>false</includeTestSourceDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle-validate</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
